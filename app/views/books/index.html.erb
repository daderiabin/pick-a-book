<h1>Books</h1>

<% if @books.count == 0 %>
  <h2>Sorry, no books here yet.</h2>
<% else %>
  <ul>
    <% @books.each do |book| %>
      <li>
        <%= link_to book.title, book %>
      </li>
    <% end %>
  </ul>
<% end %>
