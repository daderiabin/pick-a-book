<h1>Genres</h1>

<% if @genres.count == 0 %>
  <h2>Sorry, no genres here yet.</h2>
<% else %>
  <ul>
    <% @genres.each do |genre| %>
      <li>
        <%= link_to genre.name, genre %>
      </li>
    <% end %>
  </ul>
<% end %>
